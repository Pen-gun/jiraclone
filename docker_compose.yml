services:
  project-management:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    depends_on:
      - database

  database:
    image: postgres:latest
    env_file:
      - .env
    ports:
      - "5432:5432"